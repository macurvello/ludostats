package model;

public interface ConsistentData {

	/**
	 * Checks if the data is consistent or not.
	 * @return true if the data is consistent, false otherwise.
	 */
	public boolean isConsistent();
}
